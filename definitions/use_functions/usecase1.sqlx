config {
  type: "table",
  schema: "Nandu",
  name: "Function_usage"
}

js {
  const common = require("../../includes/common_functions");
}

SELECT
  employee_id,
  ${common.castColumn("employee_id", "STRING")} AS employee_id_str,
  ${common.addAuditColumns()}
FROM ${ref("employees")}